package main

import (
	"fmt"
)

func main() {
	var words = []string{"Golang", "is", "a", "very", "good", "language"}
	for i := 0; i < 6; i++ {
		fmt.Print(words[i])
		fmt.Print(" ")
	}
	fmt.Println("")
	golang()
}

func golang() {
	extrawords := []string{"language", "good", "very", "a", "is", "Golang"}
	for i := 5; i >= 0; i-- {
		fmt.Print(extrawords[i])
		fmt.Print(" ")
	}
}
