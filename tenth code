package main

import (
	"fmt"
)

func main() {
	fmt.Print("Enter a text : ")
	var input string
	fmt.Scanln(&input)
	
	fmt.Print("Reversed Text: ")
	for i := len(input) - 1; i >= 0; i-- {
		fmt.Print(string(input[i]))
	}
	fmt.Println() 
}
