package main

import "fmt"

// var b int = 11

func main() {
	name, age := "Sultan Mehmed the Conqueror", 21
	fmt.Print(name, " conquered istanbul at the age of ", age, " .\n")
	fmt.Println(name, "conquered istanbul at the age of", age, ".")
	fmt.Printf("%s conquered istanbul at the age of %d .", name, age)

	// VISIBILITY
	/*
 var a int=11
 fmt.Println(a)
 myFunc()
 */

	/*
		var c string // count, coin, costumer
		// CamelCase = Camel case is the practice of writing phrases without spaces or punctuation and with capitalized words.
		// The format indicates the first word starting with either case, then the following words having an initial uppercase letter.
		// Common examples include YouTube, iPhone and eBay.
		var coinType string
		var custName string
		// Abbreviations should be capitalized
		var URL  // not url
		var HTTP // not http
	*/

}

/*
func myFunc() {
	//fmt.Println(a)
	fmt.Printf(b)
}
*/
